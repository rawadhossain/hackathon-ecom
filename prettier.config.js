module.exports = {
	tabWidth: 4, // Sets tab width to 4 spaces
	useTabs: true, // Uses tabs instead of spaces
	semi: true, // Adds semicolons at the end of statements
	singleQuote: true, // Enforces single quotes instead of double quotes
	bracketSpacing: true,
	trailingComma: 'es5',
	printWidth: 100,
};
